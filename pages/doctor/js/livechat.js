// Custom JS for Live Chat
document.addEventListener('DOMContentLoaded', () => {

    setInterval(() => {
        // Poll for new messages
        // if new message: document.getElementById('notify-sound').play();
    }, 3000);

});