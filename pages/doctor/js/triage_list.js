// Custom JS for Live Triage List
document.addEventListener('DOMContentLoaded', () => {

    setInterval(() => {
        // Fetch new records via AJAX
        console.log('Refreshing triage list...');
    }, 30000);

});